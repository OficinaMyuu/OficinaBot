package ofc.bot.domain.entity.enums;

/**
 * Identifies which scheduled voice-channel reward a channel income rule affects.
 */
public enum VoiceChannelIncomePayoutType {
    MONEY,
    LEVEL_EXPERIENCE
}
